the "Orbital Spiral Blaster."Weapon Concept: The projectile consists of 3 swirling sub-spheres that orbit around a moving central path vector. They use the exact same mesh/model as the player orb, but scaled to $75\%$ ($0.75$) of the player's size. The overall projectile maintains strict constant linear direction and speed throughout its lifespan, regardless of hitting targets. It can hit up to 3 targets total: each time a sub-sphere collides with an enemy, one sub-sphere is destroyed/consumed, dealing damage to that target, while the remaining sub-spheres continue moving on the exact same trajectory until all 3 are gone or it expires.Please write the code/logic to execute this behavior with high-impact AAA game feel:Spawn & Geometry Setup:Instantiate a main empty parent GameObject/Node to handle straight-line movement vector ($\vec{v}$).Instantiate 3 child orb meshes (using the player orb model, scaled to $0.75$) spaced $120^{\circ}$ apart around the center axis.Drive their rotation in the update loop so they continuously swirl/spiral around the central movement vector at a fixed distance.Continuous Trajectory Logic:Move the parent origin at a fixed speed along the firing vector. Do not apply bounce, deflection, or velocity drop when a hit occurs.Hit Tracking & Sub-Sphere Destruction:Attach a trigger collider to each of the 3 sub-spheres (or handle raycasting/overlap checks per sub-sphere).When a sub-sphere contacts a valid target: trigger damage on the target, spawn a localized impact FX at that sub-sphere's exact location, and immediately destroy/disable that individual child orb mesh and collider.Recalculate or smoothly re-balance the remaining orbital radius/speed if desired, or keep the remaining orbs on their fixed $120^{\circ}$ offset track.When the target count hits $3$ (all sub-spheres destroyed), destroy the parent projectile container.Orbiter Launch Recoil:Upon firing, perform a brief "pull-back and snap" animation on the player orb ($X: 1.2, Y: 0.8$ squash for $0.06\text{s}$) along with a clean backward impulse force ($-\vec{v}$).Collision Setup:Ensure all 3 sub-spheres ignore collision with the caster player orb entirely.FX Hooks:Attach glowing, swirling ribbon trails to each of the 3 sub-spheres that intertwine in a helix/DNA shape as they move.Trigger a distinct "shatter/implosion" particle burst whenever an individual sub-orb is lost to a target.